import { createTheme  } from '@mui/material/styles';
import { ruRU } from '@mui/material/locale';
export const theme = createTheme({
  palette: {
    primary: {
      main: '#263b5e'
    },
  },
}, ruRU);

export const backgroundColor05 = 'rgba(38, 59, 94, 0.5)';